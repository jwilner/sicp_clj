(ns sicp-clj.ch2.60)

;; just change union-set to append, adjoin-set to conj?
;; element-of-set is still O(n)
;; adjoin-set becomes O(1)
;; intersection-set is just O(n2)
;; union-set changes to whatever append is
